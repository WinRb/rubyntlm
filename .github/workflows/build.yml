---
name: build

"on":
  pull_request:
  push:
    branches:
      - main

# The job only ever reads the checkout; deny everything else by default.
permissions:
  contents: read

# A new push to a PR makes the in-flight run redundant. Never cancel main,
# because those runs are what the README badge reports.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ruby/setup-ruby@a0102e0972be65f351c307e2d64b9314a57c8073 # v1.324.0
        with:
          ruby-version: "3.4"
          bundler-cache: true
      - name: Audit dependencies
        run: bundle exec bundle-audit check --update
      - name: Audit Ruby runtime
        # CVE-2026-41316 is an ERB flaw fixed in Ruby 4.0.3 and not yet backported.
        # Ignored on affected rubies only. See:
        # https://www.ruby-lang.org/en/news/2026/04/21/erb-cve-2026-41316/
        # https://github.com/advisories/GHSA-q339-8rmv-2mhv
        run: |
          ignore=""
          if ruby -e 'exit 1 unless Gem::Version.new(RUBY_VERSION) < Gem::Version.new("4.0.3")'; then
            ignore="--ignore CVE-2026-41316"
          fi
          bundle exec ruby-audit check $ignore

  zizmor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Audit workflows
        uses: zizmorcore/zizmor-action@cc914d7f3750a2d13d75c7f184a1060aa0e9d482 # v0.6.4
        with:
          advanced-security: false # fail the build on findings instead of uploading SARIF

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ruby/setup-ruby@a0102e0972be65f351c307e2d64b9314a57c8073 # v1.324.0
        with:
          ruby-version: "3.4"
          bundler-cache: true
      - name: RuboCop
        run: bundle exec rubocop --format github

  unit:
    name: ${{ matrix.os }} / ruby ${{ matrix.ruby }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    # Nightly builds break for reasons that are not this gem's fault, so they
    # report without failing the run.
    continue-on-error: ${{ matrix.experimental || false }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        ruby: ['3.0', '3.1', '3.2', '3.3', '3.4', '4.0']
        include:
          - os: ubuntu-latest
            ruby: 'head'
            experimental: true
          - os: windows-latest
            ruby: 'head'
            experimental: true
          # truffleruby has no Windows build via setup-ruby.
          - os: ubuntu-latest
            ruby: 'truffleruby'
          - os: ubuntu-latest
            ruby: 'truffleruby-head'
            experimental: true
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: ruby/setup-ruby@a0102e0972be65f351c307e2d64b9314a57c8073 # v1.324.0
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true

      - name: Run specs
        if: matrix.os != 'ubuntu-latest' || matrix.ruby != '3.4'
        run: bundle exec rake spec

      - name: Run specs with coverage
        if: matrix.os == 'ubuntu-latest' && matrix.ruby == '3.4'
        run: bundle exec rake coverage

      - name: Upload coverage to Coveralls
        if: matrix.os == 'ubuntu-latest' && matrix.ruby == '3.4'
        uses: coverallsapp/github-action@8d6379e14d29928660c4ba802d8e85393440b329 # v2.3.8
        with:
          file: coverage/.resultset.json
          format: simplecov

  frozen-string-literal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ruby/setup-ruby@a0102e0972be65f351c307e2d64b9314a57c8073 # v1.324.0
        with:
          ruby-version: "3.4"
          bundler-cache: true
      # Regression guard for the frozen-string-literal compatibility work in
      # #64: runs the whole suite with string literals frozen process-wide, so
      # any future mutation of a frozen literal fails loudly here instead of
      # on user machines running Ruby with --enable-frozen-string-literal.
      - name: Run specs with frozen string literals
        env:
          RUBYOPT: "--enable-frozen-string-literal --debug-frozen-string-literal"
        run: bundle exec rake spec
